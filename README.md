
  # City Promotional Website UI

  This is a code bundle for City Promotional Website UI. The original project is available at https://www.figma.com/design/0rdFDimM8LcoHTr5lkg3WC/City-Promotional-Website-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  